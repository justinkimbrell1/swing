
  
    // Function to show the modal after a delay
    function showModalAfterDelay() {
        jQuery('#exampleModalCenter').modal('show');
    }

    // Set a timeout to call the function after a delay (in milliseconds)
    setTimeout(showModalAfterDelay, 10); // Adjust the delay time (5000 milliseconds = 5 seconds)

    function showSection() {
        document.getElementById("hiddenSection").style.display = "block";
      }
      setTimeout(showSection, 1913000); 
